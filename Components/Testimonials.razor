@using Radzen
@using Radzen.Blazor
@using BlazorBFA.ViewModels
@inject TestimonialsViewModel ViewModel

<section id="testimonial" class="section-screen pt-16 bg-secondary text-secondary-foreground relative flex flex-col justify-between pb-20">
    <div class="absolute inset-0 opacity-10 bg-[radial-gradient(ellipse_at_center,_var(--tw-gradient-stops))] from-primary via-transparent to-transparent"></div>

    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 relative z-10 flex flex-col gap-10 w-full">

        <!-- Header -->
        <div class="text-center max-w-3xl mx-auto reveal">
            <div class="inline-flex items-center gap-2 mb-4 justify-center w-full">
                <span class="h-px w-8 bg-primary"></span>
                <h3 class="text-primary font-semibold tracking-wider uppercase text-sm">Testimonial</h3>
                <span class="h-px w-8 bg-primary"></span>
            </div>
            <h2 class="text-4xl md:text-5xl font-display font-bold text-white mb-6">Cosa <span class="text-transparent bg-clip-text bg-gradient-to-r from-primary to-emerald-300">dicono di noi</span></h2>
        </div>

        <!-- Cards row -->
        <div class="flex items-center gap-4">
            <button type="button" class="shrink-0 w-11 h-11 rounded-full border border-white/20 flex items-center justify-center text-white hover:bg-primary hover:border-primary transition-all" @onclick="Previous" aria-label="Previous">
                <RadzenIcon Icon="chevron_left" style="font-size:1.4rem;" />
            </button>

            <div class="flex-1 grid grid-cols-1 md:grid-cols-3 gap-6 overflow-hidden @_slideClass">
                @for (var offset = -1; offset <= 1; offset++)
                {
                    var model = GetRelative(offset);
                    var isActive = offset == 0;

                    <article class="bg-white/5 rounded-2xl p-8 backdrop-blur-sm relative transition-all duration-300 @(isActive ? "scale-100 opacity-100" : "scale-95 opacity-50")"
                             style="border: @(isActive ? "1px solid hsl(var(--primary) / 0.55)" : "1px solid rgba(255,255,255,0.15)");">

                        <svg style="position:absolute;top:1.25rem;right:1.25rem;width:2.2rem;height:2.2rem;color:rgba(255,255,255,0.12);" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
                            <path d="M3 21c3 0 7-1 7-8V5c0-1.25-.756-2.017-2-2H4c-1.25 0-2 .75-2 2v7c0 1.25.75 2 2 2h1c1 0 1 .63 1 1.5A1.5 1.5 0 0 1 5 17c-1 0-1 .08-1 1.25V21"/>
                            <path d="M15 21c3 0 7-1 7-8V5c0-1.25-.756-2.017-2-2h-4c-1.25 0-2 .75-2 2v7c0 1.25.75 2 2 2h1c1 0 1 .63 1 1.5A1.5 1.5 0 0 1 17 17c-1 0-1 .08-1 1.25V21"/>
                        </svg>

                        <!-- Stars -->
                        <div class="flex gap-1 mb-6">
                            @for (var i = 0; i < 5; i++)
                            {
                                <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"
                                     style="@(i < model.Rating ? "fill:hsl(var(--primary));stroke:hsl(var(--primary));" : "fill:none;stroke:rgba(255,255,255,0.3);")">
                                    <polygon points="12 2 15.09 8.26 22 9.27 17 14.14 18.18 21.02 12 17.77 5.82 21.02 7 14.14 2 9.27 8.91 8.26 12 2"/>
                                </svg>
                            }
                        </div>

                        <!-- Quote text -->
                        <blockquote class="leading-relaxed mb-6 min-h-[120px] italic" style="font-size:0.9rem;color:rgba(255,255,255,0.82);">@model.Content</blockquote>

                        <!-- Author -->
                        <div>
                            <h3 class="font-bold text-white text-base">@model.Name</h3>
                            <p class="text-sm mt-0.5" style="color:hsl(var(--primary));">@model.Role</p>
                        </div>
                    </article>
                }
            </div>

            <button type="button" class="shrink-0 w-11 h-11 rounded-full border border-white/20 flex items-center justify-center text-white hover:bg-primary hover:border-primary transition-all" @onclick="Next" aria-label="Next">
                <RadzenIcon Icon="chevron_right" style="font-size:1.4rem;" />
            </button>
        </div>

        <!-- Dots -->
        <div class="flex justify-center gap-2.5 mt-10">
            @for (var index = 0; index < ViewModel.Testimonials.Count; index++)
            {
                var idx = index;
                <button type="button"
                        class="h-2.5 rounded-full transition-all duration-300 @(index == currentIndex ? "bg-primary w-8" : "w-2.5")"
                        style="@(index == currentIndex ? "" : "background-color:rgba(255,255,255,0.38);")"
                        @onclick="async () => await GoTo(idx)"
                        aria-label="@($"Testimonianza {idx + 1}")">
                </button>
            }
        </div>

    </div>

    <ScrollIndicator TargetSection="contatti" Text="Scorri" />
</section>
